parameters:
  - name: environment
    type: string
  - name: business_unit
    type: string

stages:
  - stage:
    jobs:
      - job:
        steps:
          - script: echo ${{ parameters.business_unit }}
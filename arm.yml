parameters:
  - name: environment
    type: string
  - name: business_unit
    type: string

stages:
  - stage: ${{ parameters.environment }}
    jobs:
      - job: Deploy_${{ parameters.environment }}
        steps:
          - script: echo ${{ parameters.business_unit }}